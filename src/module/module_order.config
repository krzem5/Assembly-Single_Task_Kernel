# Keyring
keyringstore

# UI (before GPU drivers)
ui_common
ui_display
ui_permissions
opengl

# Filesystems
iso9660=early
kfs2=early

# Partition descriptors
gpt=early

# USB drivers
usb_msc=early

# PCI drivers
ahci=early
ata=early
i82540
nvme=early
virtio=early
xhci=early

# VirtIO drivers
virtio_blk=early
virtio_fs=early
virtio_gpu

# AML
aml_bus
aml_shutdown

# Shutdown
vm_shutdown

# dynamicfs
dynamicfs
devfs
procfs
sysfs

# Network layer2
net_info
net_arp
net_ip

# Network layer3
net_udp
net_unix

# Network layer4
net_dhcp
net_dns

# Debugging
lockinfo
test=test

# Filesystem loader (after all early drivers)
fs_loader=early

# Load UI after all other drivers
ui_launcher=not-test
