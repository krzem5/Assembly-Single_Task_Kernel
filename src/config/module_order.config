# Keyring
keyringstore

# Accounts
account_manager

# UI (before GPU drivers)
ui_common
ui_display
ui_permissions
opengl

# Filesystems
fat32=early
iso9660=early
kfs2=early

# Partition descriptors
gpt=early

# USB drivers
usb_msc=early

# PCI drivers
ahci=early
ata=early
i82540
nvme=early
virtio=early
xhci=early

# VirtIO drivers
virtio_blk=early
virtio_fs=early
virtio_gpu

# Shutdown
aml_shutdown
vm_shutdown

# Network layer2
net_info
net_arp
net_ip

# Network layer3
net_tcp
net_udp
net_unix

# Network layer4
net_dhcp
net_dns

# dynamicfs
dynamicfs
devfs
procfs
sysfs

# Debugging
lockinfo
test=test

# Filesystem loader (after all early drivers)
fs_loader=early

# Linux compatibility layer (after all modules)
linux_syscall

# Load UI after all other drivers
ui_launcher=not-test
